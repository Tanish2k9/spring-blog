spring:
  application:
    name: Blog-springboot
  datasource:
    url: jdbc:mysql://localhost:3306/blog_db?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
secreteJwtString: *******************
cloudinary:
  cloud_name: ******************
  api_key: ********************
  api_secret: *************************